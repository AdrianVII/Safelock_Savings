<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aa.safelocksaving">

    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafelockSaving">
        <activity android:name=".Account_Activity" android:exported="true"/>
        <activity android:name=".SignIn_Activity" android:exported="true"/>
        <activity android:name=".Register_Activity" android:exported="true"/>
        <activity android:name=".Forgot_Activity" android:exported="true"/>
        <activity android:name=".Main_Activity" android:exported="true"/>
        <!-- Principal -->
        <activity android:name=".Start_Activity" android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

    </application>

</manifest>